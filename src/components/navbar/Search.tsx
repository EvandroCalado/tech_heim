import { IconSearch } from '@/icons';
import { Button } from '../ui/button';

export const Search = () => {
  return (
    <Button variant="outline" size="icon">
      <IconSearch />
    </Button>
  );
};
